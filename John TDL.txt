create read workplan
finish create workplan
  -add a way to check if workplan number is unique
  -change date to datetime or input validate? 
  -get booking number for new workplan


create update workplan
- added a int selectedWorkplan that equals lstboxWorkplan
- needs to output selected workplan to the boxes 
finish read workplan
 =polish of the display formula , more spaces maybe
ability to sort by in workplans
finish read parts
  -create UI 
  -create JSON file
  -get it to read out
  -method to return certain parts? 
 


input validation and exception handling
improve the aethestics 

begin merge

impve the file selection to a shortened URL inside the directory

maybe add different views based on Louis's class